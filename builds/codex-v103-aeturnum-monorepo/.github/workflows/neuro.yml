name: neuro-ci
on:
  push: { branches: [ main, master ] }
  pull_request: { branches: [ main, master ] }
jobs:
  neuro:
    uses: ./.github/workflows/reusable-ci.yml
    with: { workdir: packages/neuro }
